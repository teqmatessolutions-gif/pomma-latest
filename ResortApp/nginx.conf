# Nginx configuration for TeqMates Resort Management System
# Domain: www.teqmates.com

# Rate limiting
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general:10m rate=1r/s;

# Upstream backend server
upstream resort_backend {
    server 127.0.0.1:8000;
    keepalive 32;
}

# Main server block - HTTP
server {
    listen 80;
    server_name _; # Listen to all requests (IP address)

    # SSL Configuration (DISABLED FOR IP TESTING)
    # ssl_certificate /etc/letsencrypt/live/teqmates.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/teqmates.com/privkey.pem;
    # ssl_protocols TLSv1.2 TLSv1.3;
    # ...

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Client settings
    client_max_body_size 50M;
    client_body_timeout 12;
    client_header_timeout 12;
    keepalive_timeout 15;
    send_timeout 10;

    # User App (Userend)
    location / {
        root /opt/pomma/userend-build/;
        index index.html;
        try_files $uri $uri/ /index.html;
        include /etc/nginx/mime.types;
    }

    # Admin Dashboard - /admin
    location /admin/ {
        alias /opt/pomma/dasboard-build/;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
        include /etc/nginx/mime.types;
    }

    # Legacy/Alt Admin Dashboard - /pommaadmin
    location /pommaadmin/ {
        alias /opt/pomma/dasboard-build/;
        index index.html;
        try_files $uri $uri/ /pommaadmin/index.html;
        include /etc/nginx/mime.types;
    }

    # Redirect /admin and /pommaadmin without trailing slash
    location = /admin { return 301 /admin/; }
    location = /pommaadmin { return 301 /pommaadmin/; }

    # API routes - All API calls
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_set_header Host $host;
        access_log off;
    }

    # Orchid API routes - /orchidapi/
    location /orchidapi/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://127.0.0.1:8011/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Orchid Uploads directory (Direct mapping)
    location /orchiduploads/ {
        alias /var/www/inventory/ResortApp/uploads/;
        expires 30d;
        add_header Cache-Control "public";
        add_header Access-Control-Allow-Origin *;
    }

    # Better mapping for API-based uploads
    location /orchidapi/uploads/ {
        alias /var/www/inventory/ResortApp/uploads/;
        expires 30d;
        add_header Cache-Control "public";
        add_header Access-Control-Allow-Origin *;
    }

    # Backward compatibility for cached clients
    location /orchidfiles/ {
        alias /var/www/inventory/ResortApp/;
        expires 30d;
        add_header Cache-Control "public";
        add_header Access-Control-Allow-Origin *;
    }

    # -------------------------------------------------------
    # SHARED RESOURCES & API
    # -------------------------------------------------------

    # User interface static files (likely shared or legacy)
    location /user-static/ {
        alias /var/www/resort/Resort_first/dasboard/build/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Uploads directory
    location /uploads/ {
        alias /opt/pomma/ResortApp/uploads/;
        expires 30d;
        add_header Cache-Control "public";
    }

    # Static files
    location /backend-static/ {
        alias /opt/pomma/ResortApp/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Error pages
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;

    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # Logging
    access_log /var/log/nginx/resort_access.log;
    error_log /var/log/nginx/resort_error.log;
}
